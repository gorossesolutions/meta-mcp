// Manual smoke test script — run each MCP tool directly against the Meta
// API (bypassing the MCP transport) and print the result, so tools can be
// validated one by one before wiring them into Claude Code.
//
// TODO: as each tool lands in src/tools/read and src/tools/write, add a
// corresponding call here, e.g.:
//
//   import { listAdAccounts } from "../src/tools/read/list-ad-accounts.js";
//   console.log(await listAdAccounts());
console.log("No tools implemented yet — see Etape 2 in the project plan.");
export {};
//# sourceMappingURL=manual-check.js.map